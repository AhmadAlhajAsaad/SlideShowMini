# SlideShowMini
SlideShowMini is a simple image slideshow application built using HTML, CSS, and JavaScript. It displays a main image at the top and allows the user to navigate between multiple images using navigation buttons. This project demonstrates the use of arrays, functions, and DOM manipulation in JavaScript.
